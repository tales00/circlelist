import { createStore } from 'vuex';
import app from './app';
import viewing_list from './viewing_list';

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    app,
    viewing_list,
  },
});
